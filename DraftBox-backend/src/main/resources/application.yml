spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/draftbox}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true

jwt:
  secret: ${JWT_SECRET:8f42a73054b1749f8f58848be5e6502c8f42a73054b1749}
